import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '02ComponentsApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
